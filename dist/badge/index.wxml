<view class='air-class air-badge'>
    <slot />
    <view wx:if="{{!dot}}" class='badge' style='background-color: {{bgColor}};color:{{color}};font-size:{{fontSize}}px'>
        {{finalCount}}
    </view>
    <view wx:else class="dot" style="background-color: {{bgColor}};" />
</view>
