<view class="air-class air-qrcode">
    <air-loading air-class="air-qrcode-loading" wx:if="{{loading}}" />
    <image wx:if="{{!loading}}" style="width: {{ size + 'px' }}; height: {{ size + 'px' }};" src="{{qrcodeImage}}" />
</view>